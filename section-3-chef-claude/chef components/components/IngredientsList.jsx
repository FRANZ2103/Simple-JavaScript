export default function IngredientsList(){
    return(
        <>
        
        {/* This is where the ingredients get added */}
        {/* Source of truth should be at the Parent not here     */}
        {/* We're supposed to send the added ingredient to Parent */}
        {/* Migrate first the field and button here */}
        {/* Then use onclick={() =>props.onPress(props.whichPad)}.  onpress is the function defined in Chef*/}

        {/* Lets try to migrate both field and button here */}
        {/* No wait, only ingredients list is supposed to be here */}
        

        </>
    )
}